version: 0.1
frontend:
  phases:
    preBuild:
      commands:
        - cd artesian-wells
        - yarn install
    build:
      commands:
        - npm start
  artifacts:
    baseDirectory: build
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*
