version: 0.1
frontend:
  phases:
    preBuild:
      commands:
        - cd artesian-wells
        - npm install
    build:
      commands:
        - npm start
  artifacts:
    baseDirectory: build
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*
