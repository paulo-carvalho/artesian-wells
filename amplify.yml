version: 0.1
frontend:
  phases:
    preBuild:
      commands:
        - npm install -g serve
        - cd artesian-wells
        - npm ci
    build:
      commands:
        - serve -s build
  artifacts:
    baseDirectory: build
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*
