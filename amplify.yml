version: 0.1
frontend:
  phases:
    preBuild:
      commands:
        - cd artesian-wells
        - npm ci
    build:
      commands:
        - npm run build
        - npm start
  artifacts:
    baseDirectory: build
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*
